apiVersion: v1
kind: Service
metadata:
    name: {{ .Value.product.service.name | quote }}
    namespace: {{ .Release.Namespace | quote }}
    labels:
        app: {{ Value.product.service.name | quote }}
        chart: {{ include "simple-ecommerce.chart" . }}
        release: {{ .Release.Name | quote }}
spec:
    type: ClusterIP
    ports:
        - name: http
          port: {{ .Value.product.service.port | quote }}
          targetPort: {{ .Value.product.containerPort | quote }}
    selector:
        app: {{ include "simple-ecommerce.fullname" . }}-product